import type { ReactNode } from "react"; type StructuredDataProps = { name: string; description: string; url: string; }; export default function StructuredData({ name, description, url }: StructuredDataProps): ReactNode { const structuredData = { "@context": "https://schema.org", "@type": "WebApplication", name, url, description, applicationCategory: "UtilitiesApplication", operatingSystem: "Web", offers: { "@type": "Offer", price: "0", priceCurrency: "USD" } }; return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />; }
